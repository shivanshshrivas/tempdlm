# Normalize all text files to LF in the repo and working tree.
# This ensures Prettier (endOfLine: "lf") and CI (Linux) agree with
# local dev on Windows — no spurious CRLF-only diffs.
* text=auto eol=lf

# Binary files — never touch line endings
*.png binary
*.ico binary
*.icns binary
*.exe binary
*.dll binary
